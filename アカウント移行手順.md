# 配信の引き継ぎ・Expo アカウント移行手順

このアプリは **Expo Go + EAS Update（OTA配信）** で運用しています。

- **使う人（研究室メンバー全員）**：Expo Go で **QR / リンクを開くだけ**。Expo アカウントの**ログインは不要**。
- **配信する人（1人でOK）**：Expo アカウントが必要。`eas update` で最新版を配信する。
- **データ（水槽・餌やり等）**：Google Sheets 同期で共有。**Expo アカウントとは無関係**なので、配信担当が誰でも同期URLを共有していれば引き継がれる。

配信元のアカウント情報は **`app.config.ts` の先頭2定数**に集約しています。

```ts
const EAS_OWNER = 'haruki2y489329480';
const EAS_PROJECT_ID = '6ae37cf7-6de6-497f-b03c-3de69f2660ff';
```

`updates.url`（`https://u.expo.dev/<projectId>`）は `EAS_PROJECT_ID` から自動生成されるので、個別に直す必要はありません。

---

## A. 新しい配信担当（友達）の初回セットアップ

### A-1. GitHub リポジトリへのアクセス
1. リポジトリのオーナー（現: `harukikamei-sudo`）が、友達を **Collaborator に招待**
   （GitHub → リポジトリ → Settings → Collaborators → 友達のGitHubユーザー名）
2. 友達が招待を **承諾（Accept invitation）**
   - GitHub の通知 / 届いたメール / `https://github.com/harukikamei-sudo/zebrafish-mobile/invitations` から

### A-2. コードを取得してセットアップ
```bash
git clone https://github.com/harukikamei-sudo/zebrafish-mobile.git
cd zebrafish-mobile
npm install
npm install -g eas-cli   # まだ入っていなければ
```

---

## B. 配信元アカウントを友達のものに移す

### B-1. 友達の Expo アカウントでプロジェクトを作る
```bash
eas login        # ← 友達のアカウントでログイン
eas whoami       # アカウント名を確認
eas init         # ← 新しい Project ID が発行される
```
※ `eas init` が `app.json` を書き換えようとしても、このアプリは `app.config.ts` が優先です。最終的に下の B-2 を正しくすればOK。
Project ID は Expo 管理画面（expo.dev → 該当プロジェクト → Settings）でも確認できます。

### B-2. `app.config.ts` の2定数を書き換える
```ts
const EAS_OWNER = '友達のアカウント名';
const EAS_PROJECT_ID = 'eas init で出た新しいID';
```

### B-3. 設定を確認
```bash
npx expo config --type public
```
`owner` / `extra.eas.projectId` / `updates.url` が友達の値になっていればOK。

### B-4. 配信
```bash
eas update --branch preview --message "研究室アカウントへ移行"
```

### B-5. 新しい QR / リンクを配り直す
Project ID が変わると配信先（QR / リンク）も変わります。
**新しいアカウント／プロジェクトの preview ブランチの QR・リンク**を、研究室メンバーに配り直してください。

---

## C. 以降の運用（コードを更新したら配信する）
1. コードを直したら GitHub に push（`git push origin main`）
2. 配信担当が最新を取得して配信：
   ```bash
   git pull
   eas update --branch preview --message "変更内容を一言で"
   ```
3. メンバーは Expo Go を開き直す（ダウンロード→閉じて再度開く）と最新が反映される

> ⚠️ `--message` は必ず1行で。途中で改行するとメッセージが壊れます。

---

## 補足
- `runtimeVersion` は `sdkVersion` ポリシー。Expo SDK を上げない限り、既存の Expo Go から OTA で更新できます。
- 配信ブランチは **`preview`**（複数人が同じQRから利用）。
- **dev server（`expo start`）は立ち上げません**。サーバー不要・QR配布の運用を壊さないこと。
- アプリ本体の設定（name / slug / icon / plugins 等）は `app.json`。アカウント関連だけ `app.config.ts` が上書きします。
- 複数人が配信もできるようにしたい場合は、Expo の **Organization（チーム）** にプロジェクトを移すのが正攻法です。

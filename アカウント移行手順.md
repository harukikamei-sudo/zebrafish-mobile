# Expo アカウント移行手順（EAS Update の配信元を別アカウントへ）

このアプリは **Expo Go + EAS Update（OTA配信）** で運用しています。
配信元のアカウント情報は **`app.config.ts` の先頭2定数**に集約してあります。

```ts
const EAS_OWNER = 'haruki2y489329480';
const EAS_PROJECT_ID = '6ae37cf7-6de6-497f-b03c-3de69f2660ff';
```

`updates.url`（`https://u.expo.dev/<projectId>`）は `EAS_PROJECT_ID` から自動生成されるので、個別に直す必要はありません。

## 移行手順

1. **新アカウントでログイン**
   ```
   eas logout        # 念のため現アカウントからログアウト
   eas login         # 新アカウントでログイン
   eas whoami        # 新アカウント名を確認
   ```

2. **新アカウントでプロジェクトを用意し、Project ID を取得**
   ```
   eas init          # 新しい Project ID が発行される
   ```
   ※ `eas init` が設定ファイルを書き換えようとした場合、`app.config.ts` は定数管理なので**手動で反映**してください（次の手順）。
   Project ID は Expo ダッシュボード（該当プロジェクトの Settings）でも確認できます。

3. **`app.config.ts` の2定数を書き換える**
   ```ts
   const EAS_OWNER = '新しいアカウント名';
   const EAS_PROJECT_ID = '新しいProjectID';
   ```

4. **設定が正しいか確認**
   ```
   npx expo config --type public
   ```
   `owner` / `extra.eas.projectId` / `updates.url` が新しい値になっていればOK。

5. **配信**
   ```
   eas update --branch preview --message "アカウント移行"
   ```

6. **新しい QR / リンクを配り直す**
   配信元（Project ID）が変わるため、**旧QRは旧アカウントの配信を指したまま**です。
   新しいアカウント／プロジェクトの **preview ブランチのQR・リンク**を、利用者に配り直してください。

## 補足
- `runtimeVersion` は `sdkVersion` ポリシーなので、Expo SDK を上げない限り、既存の Expo Go から OTA で更新できます。
- 配信ブランチは **`preview`** を使っています（複数人が同じQRから利用）。
- アプリ本体の設定（name / slug / icon / plugins 等）は `app.json` にあります。アカウント関連だけ `app.config.ts` が上書きします。

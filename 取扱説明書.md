# 🐟 ゼブラフィッシュ水槽管理アプリ — 取扱説明書

研究室の水槽・餌やり・交配・産卵成績をスマホで管理できるアプリです。
**オフラインでも動作**し、オンライン時に **Google スプレッドシートで全員のデータを共有**します。

---

## 1. インストール（iPhone）

1. App Store で **「Expo Go」** を検索してインストール
2. 配布された **QR コード**（`expo-go-invite.png`）を **iPhone の標準カメラ**で読み取る
3. 上に出るバナーをタップ → **Expo Go が開いてアプリが起動**します

> モバイル回線（4G/5G）でOK。Wi-Fi が無くても起動できます。
> 初回はアプリの読み込みに少し時間がかかります（次回以降は速くなります）。

---

## 2. データ共有の設定（初回だけ）

全員が同じスプレッドシートを共有するため、最初に一度だけ設定します。

1. 下のタブ **「その他」→「設定」** を開く
2. **「データ同期」** の「同期先URL」に、管理者から共有された URL を貼り付け
   ```
   同期URL：（ここに管理者から共有されたURLを貼る）
   ```
   ※ 合言葉（SECRET）の指定があれば「共有トークン」にも入力
3. **「接続テスト」** → ✅ になればOK
4. **「今すぐ同期」** → みんなのデータが取り込まれます

> 担当者名（設定の一番上）を入れておくと、操作ログに名前が残ります（任意）。

---

## 3. 画面の使い方

下のタブで切り替えます。

### 🏠 ホーム
- あいさつ・天気（晴れ/雨で色が変わります）・本日の餌やり状況
- **概況タイル**（水槽数・総匹数・産卵記録・隔離中・進行中）はタップで各ページへ
- 最近のアクティビティ（タップで開閉）
- 一番下に **「今すぐ同期」** ボタン

### 🦐 餌やり
- 目標は1日4回。前回からの経過時間で色が変化（赤=要給餌）
- **「全水槽に餌をあげた」** で記録。直近の1件は取り消し可能
- 過去ログ・CSV書き出しもここ

### 💕 交配
- 「新規トライアルを計画」でラックを選び、♂♀の元水槽・交配槽・タグを指定して一括登録
- 進行中トライアルは **計画中 → 前日セット → 採卵 → 戻し** とボタンで進める
- 採卵結果（卵数・受精率）を入れると産卵成績に自動反映。受精率の推移グラフも表示

### 🪣 水槽
- 場所（ラック・段・列）を選ぶと水槽IDが自動生成 → 匹数・系統・健康状態・メモを登録/更新
- 2水槽の中身スワップ、フィルタ、CSV入出力、削除
- 登録済み一覧が下に並びます

### その他 → 📐 棚ビュー
- ラック×段×列をマス目で表示。色＝健康状態。マスをタップで詳細

### その他 → 📈 成績分析
- ♂/♀/ペア別の成功率ランキング、次回おすすめペア Top5

### その他 → 📔 ログ
- 誰がいつ何をしたかの履歴。種別・期間・キーワードで絞り込み、CSV書き出し

### その他 → ⚙️ 設定
- 担当者名、データ同期、ラック/段の追加・削除

---

## 4. オフラインと同期について

- **オフラインでも全機能が使えます**。記録は端末に保存され、消えません
- オンラインに戻ると **自動で同期**します（アプリ起動時・復帰時・数分ごと）
- うまく同期されていないと感じたら、ホーム下部の **「今すぐ同期」** を押してください
- 画面が古いまま見えるときは、**下に引っぱって更新** するか、アプリを開き直してください

### 複数人で使うときのコツ
- 同期は **レコード単位で「更新時刻が新しい方」を採用**します
- **別々の水槽を担当**すれば衝突しません。**同じ1つの水槽を同時に編集**すると、後から保存した方が優先されます
- 削除も全員に反映されます

---

## 5. 困ったとき（FAQ）

| 症状 | 対処 |
|------|------|
| 起動時に「incompatible with Expo Go」 | Expo Go アプリを最新に更新。直らなければ管理者に連絡（対応SDKを合わせます） |
| 「Opening project」でずっとクルクル | 電波の良い場所で再試行。それでも続くなら管理者に連絡 |
| 同期が失敗する | オンラインか確認／設定の同期URLに余計な空白が入っていないか確認 |
| 同期したのに反映されない | 下に引っぱって更新、またはアプリを開き直す |
| 水槽が表示されない | 「今すぐ同期」→ 画面を引っぱって更新 |

---

## 6. 注意

- **同期URL（と合言葉）を知っている人は誰でもデータを読み書きできます**。SNS等での公開は避け、メンバー内だけで共有してください。
- データの実体は Google スプレッドシートにもあります。バックアップや一括編集はスプレッドシート側でも可能です。

---

Powered by **Haruki Kamei** · **Kusayama Daichi**
